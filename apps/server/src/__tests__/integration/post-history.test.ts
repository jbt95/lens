describe('When creating a history record', () => {
	describe('and the body is valid', () => {
		it('should 200', () => {});
	});

	describe('and the body is malformed', () => {
		describe('and the date is missing', () => {
			it('should return a 400', () => {});
		});
		describe('and the date is invalid', () => {
			it('should return a 400', () => {});
		});
		describe('and the d is missing', () => {
			it('should return a 400', () => {});
		});
		describe('and the d is invalid', () => {
			it('should return a 400', () => {});
		});
		describe('and the n is missing', () => {
			it('should return a 400', () => {});
		});
		describe('and the n is invalid', () => {
			it('should return a 400', () => {});
		});
		describe('and the r1 is missing', () => {
			it('should return a 400', () => {});
		});
		describe('and the r1 is invalid', () => {
			it('should return a 400', () => {});
		});
		describe('and the r2 is missing', () => {
			it('should return a 400', () => {});
		});
		describe('and the r2 is invalid', () => {
			it('should return a 400', () => {});
		});
	});
});
